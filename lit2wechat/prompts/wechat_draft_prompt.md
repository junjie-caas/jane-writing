# 公众号初稿提示词（固定模板）

请基于 `themes/` 中已完成核验的证据，输出中文公众号初稿。

## 硬性约束
1. 只能使用已在 `notes_json/` 中出现且带出处的数字。
2. 所有数字后附来源（`paper_id` + 页码）；缺失则写“待人工核实”。
3. 明确区分“相关”与“因果”，禁止混用。
4. 必须包含“局限性/争议”段落。
5. 风格要求：专业、清晰、少空话。

## 建议结构
- 背景问题
- 核心发现（含数字与出处）
- 机制解释（如仅相关需明示）
- 局限性/争议
- 实践启发
- 参考文献（列出 `paper_id`）
